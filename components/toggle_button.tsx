const ToggleButton = () => {
  return (
    <a href="#" className="burger">
      <span className="burger-bar" />
      <span className="burger-bar" />
      <span className="burger-bar" />
    </a>
  );
};

export default ToggleButton;
